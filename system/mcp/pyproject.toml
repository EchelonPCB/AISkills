[project]
name = "aiskills-mcp"
version = "0.1.0"
description = "Local MCP server exposing governed AISkills routing and validation tools."
requires-python = ">=3.10"
dependencies = [
  "mcp[cli]>=1.2.0",
]

[tool.uv]
package = false
